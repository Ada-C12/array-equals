# Determines if the two input arrays have the same count of elements
# and the same integer values in the same exact order
def array_equals(array1, array2)
  
  array1_length = 0
  until array1[i] == nil
    array1_length += 1
  end
  
  if array1_length == 0
    # this means that this is an empty array
  end
  
  array2_length = 0
  until array[i] == nil
    array2_length += 1
  end
  
  
  
  # if x
  # return true
  # else
  # return false
  raise NotImplementedError
end
